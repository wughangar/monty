#include "monty.h"

/**
 * nop - functions that does nothing
 * @stack: pinter to top value of stack
 * @line_number: line number
 *
 * Return: void
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
